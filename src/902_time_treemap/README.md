# Time Treemap Generator

Generate interactive Plotly treemaps from SuperProductivity task data.

## Quickstart

```bash
uv run time-treemap
```

## Options

```bash
uv run time-treemap --help
uv run time-treemap -i /path/to/super_productivity.json -o ./output_dir
```
