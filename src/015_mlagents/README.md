# Marble-maze gymnasium wrapper

## Quickstart

MLAgents requires an old version of python which requires:

```bash
sudo pacman -S --needed libxcrypt libxcrypt-compat
```

## Environment

Action space: Box(-1.0, 1.0, (2,), float32)
Observation space: Box(-inf, inf, (280,), float32) (why?)
